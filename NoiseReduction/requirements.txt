
# Core runtime dependencies
# Use this on macOS, Linux, or Windows for CPU/MPS/standard installs.
torch>=2.0.0
astropy>=5.0
numpy>=1.24

# Optional XISF support for PixInsight-native files:
# pip install -r requirements-optional.txt

# For Windows with NVIDIA CUDA (recommended for GPU acceleration):
# Use the GPU-specific requirements which reference the PyTorch CUDA index:
#   pip install -r requirements-windows-cuda.txt
# NOTE: CUDA wheels require a supported Python version (CPython 3.11 or 3.12).
